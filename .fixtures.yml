---
fixtures:
  repositories:
    stdlib:
      ref: "4.24.0"
      repo: "https://github.com/puppetlabs/puppetlabs-stdlib"
    mysql:
      ref: "3.11.0"
      repo: "https://github.com/puppetlabs/puppetlabs-mysql"
    staging:
      ref: "v2.2.0"
      repo: "https://github.com/voxpupuli/puppet-staging"
  symlinks:
    vision_mysql: '#{source_dir}'
